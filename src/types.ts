export type Route = {
  path: string;
  label: string;
};

export type Testimonial = {
  id: number;
  name: string;
  content: string;
  rating: number;
};

export type SocialLinks = {
  instagram?: string;
};